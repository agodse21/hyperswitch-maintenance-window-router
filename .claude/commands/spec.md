---
description: Write or update SPEC.md for the hyperswitch-maintenance-window-router routing feature before any implementation.
---

Read `REQUIREMENTS.md` and `SPEC.template.md`.

1. Invoke workflow from skill `hyperswitch-spec` (choose ONE routing logic).
2. Create or update `SPEC.md` with full sections filled.
3. Do not write implementation code in this step.
4. Ask user to confirm routing choice only if ambiguous; otherwise default to adaptive retry.

Output: link to `SPEC.md` and the 3 test cases from the matrix.
